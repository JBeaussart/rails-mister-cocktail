<div class="container">
  <div class="row">
    <% @cocktails.each do |cocktail| %>
      <h1 class="m-auto"><%= link_to "#{cocktail.name}", cocktail_path(cocktail) %></h1>
    <% end %>
  </div>
  <%= link_to 'Add new !', new_cocktail_path, class: "btn btn-warning" %>
</div>
